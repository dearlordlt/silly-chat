Search the document(s) the user attached to this message — and any files attached to this
project — for passages relevant to a query, using semantic search over their contents.
Pass a focused query describing what you need to find (use the user's wording or key
terms). Returns the most relevant passages. Use this to answer any question that could
depend on those documents — do NOT answer from memory about their contents. Search more
than once with different queries if one pass isn't enough. If there's nothing to search it
says so.
