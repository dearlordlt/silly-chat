Search the document(s) the user attached for passages relevant to a query, using semantic
search over the document's contents. Pass a focused query describing what you need to find
(use the user's wording or key terms). Returns the most relevant passages. Use this to
answer any question about an attached document — do NOT answer from memory about its
contents. Search more than once with different queries if one pass isn't enough. If nothing
is attached it says so.
