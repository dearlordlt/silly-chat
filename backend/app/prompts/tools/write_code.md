Write code for a programming task using the dedicated coding model. Pass the full task
description and the language (e.g. "html", "python", "javascript"). Use this whenever the
user asks you to build, write, or fix code. The generated code is shown to the user
automatically as a code block — keep your own reply short (a one-line intro) and do NOT
paste the code yourself.
