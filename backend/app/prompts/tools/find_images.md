Find images for a query and get back image URLs with titles. Put any identity (names,
places, objects) directly in the query. If the user wants images that visually show a
specific attribute, set must_show to just that attribute (e.g. "a hat") — each candidate
is then checked with a vision model and only confirmed matches are returned.
