Research one or more subtasks in parallel and get back grounded findings. Pass a list of
focused subtasks — each runs as its own worker agent that searches the web. Use a single
subtask for a simple lookup, or several (one per angle) for a broad or comparative
question. Decide the decomposition yourself based on what the user asked.
