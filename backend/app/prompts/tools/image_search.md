Search the web for images and return candidate image URLs with their metadata (title,
source page). Put identifying information directly in the query (names, places, objects).
Metadata is often wrong or silent about visual details, so treat results as candidates to
be verified with vision_verify, not as confirmed matches.
